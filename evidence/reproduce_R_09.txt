=== R-09 AS-OF JOIN & 3-COMPONENT MARKOUT DECOMPOSITION AUDIT CHECK ===
Command: python3 -m unittest tests.test_harness_integrity.TestHarnessIntegrity.test_as_of_join_sparse_bbo
Timestamp: 2026-09-20T01:23:30 UTC
Status: CONFIRMED & FIXED

Finding Summary:
Prior implementation used np.searchsorted(bbo_ts, target) looking forward in time, and tolerance up to 5s.
Remediation per Mandate v2 Section 2 (R-09):
1. Strict as-of join: idx = int(np.searchsorted(bbo_ts, target_ts, side="right")) - 1 (last BBO <= target_ts).
2. Staleness drop: if staleness_sec > min(1.0, 0.5 * h), horizon observation is dropped.
3. 3-Component Decomposition:
   - spread_capture = side * (mid_fill - price) / ref_p
   - drift = side * (future_mid - mid_fill) / ref_p
   - total = side * (future_mid - price) / ref_p = spread_capture + drift
   - adverse_selection = - drift

Test Output:
Ran 1 test in 0.002s
OK
Sparse BBO exceeding tolerance dropped (N=0 for h=0.1s when gap > 50ms).
Dense BBO within tolerance preserved (N=1 for h=0.1s when gap <= 50ms).
Drift, spread capture, and total return reported.
